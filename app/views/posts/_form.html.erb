
<div class="col-md-3"></div>
<div class="col-md-6"</div>

<%= bootstrap_form_for @post  do |f| %>

	<%= f.text_field :title %>

 	<%= f.text_area :description %>



		<%= bootstrap_nested_form_for :recipients do |b|%>
			<%= b.text_field :email %>

		
	<%#= f.collection_check_boxes :recipient_ids, Recipient.all, :id, :name %>

	<%#= f.text_field :description, 'data-role'=>'tagsinput' %>

		<center>
	  		<%= f.submit %>
		</center>
	<% end %>
 	
<% end %>
